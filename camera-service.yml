server:
  port: 8083

eureka:
  client:
    serviceUrl:
      defaultZone: http://10.10.2.113:8061/eureka/

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"
  config: log4j2.yml

springdoc:
  packagesToScan: com.comit.services.camera

spring:
  datasource:
    username: tung
    url: jdbc:mysql://10.10.2.113:3306/camera_db?autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
  zipkin:
    base-url: http://10.10.2.113:9411
  sleuth:
    sampler:
      probability: 1.0

core:
  api:
    check-camera: http://10.10.2.116:5678/camera/check_cameras